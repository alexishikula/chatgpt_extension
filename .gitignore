# Examples 1 and 2 show that only source/config files were added/modified, so no .gitignore content is needed

# Source files like .md and .js don't need to be ignored
# Only build artifacts, dependencies, temp files, etc. should be in .gitignore

# In this case, only a markdown file and a JavaScript file were changed
# These are source files that should be tracked by git